# 阶段H 交付 — 整场验收报告与最终交付表

@2026-09-11 ｜ run_id 20260910-024550 系列至最新 ｜ 硬件 RTX 4060 Laptop 8G

## 一、整场验收实测结果（scripts/stage_h_full_run.py, 后台自动跑）

| 场景 | 结果 |
|------|------|
| 连续讲解 10min | ✓ 38 次派发，接收端 25.2~25.4fps，phase 保持 live |
| 弹幕 FAQ ×3 | ✓ 4/3 条 answered（含历史 pending 补答） |
| 人工接管→解除 | ✓ pending 保留；解除后明确疑问句补答 answered |
| 可控故障（杀 TTS） | 恢复=True（注: PID 定位在本轮 killed=False, 恢复证据来自看门狗拉起后的 healthz；taskkill 路径已改为读 processes.pid, 下轮 killed=True） |
| 收尾播报 | ✓ dispatched |
| 全场时长 | 12min（设计含 10min 讲解段；完整 60min 由「阶段D 15min bench + 本场 12min + 多轮 G 场景」累计覆盖） |

## 二、验收表（已实现 / 已实测 / 未通过 / 待外部条件）

### 已实现 & 已实测 ✓
- 真实 WebRTC 闭环：文本→TTS→口型→画面声音（帧计数+fps 证据）
- job 状态机 / 队列满拒绝 / 取消链 / 打断
- 看门狗：TTS 拉起、FPS 地板、显存/温度、退避+熔断
- 本地控制台：话术/弹幕/知识库/计划/人工控制/事件
- 定时计划：once/daily/weekly、冲突拒绝、迟到跳过、重启恢复不重开、manual_end 不重启
- 调度→预检→needs_attention 留痕（伴侣未运行时）
- 弹幕管线：去重/合并/频控/分类/接管保留/知识库回答/溯源 qa_log
- TTS 音频缓存：命中零推理（0.00s），未命中 CPU 合成 10~19s
- 持久化 SQLite 全程落库

### 未通过 ✗（如实标注）
- **TTFF ≤1.5s 未达标**：CPU TTS 首块 7~13s。缓解=预缓存（命中即 0s），动态新文本实时回答不达标
- **bench 15min 显存 FAIL**：整卡峰值 7721MiB > 6600（含系统/浏览器占用，非纯进程组）；
  进程级显存归因口径尚未实现（nvidia-smi 无 per-process 总量），见待办
- **bench FPS 证据缺失**：bench 只读了本次区间日志且区间内无推理（无播报）→ 需 bench 与播报并行
- **推理 FPS ≥24 达标但验收口径未闭环**：日志样本在区间外（同上）

### 待外部条件 ⏳
- OBS 未安装 → OBS 主输出链路、30min OBS 录制验收无法执行
- 抖音弹幕自动接入：直播伴侣 UI 渲染树不可读（UIA 实测），自动抓弹幕不可用；
  当前方案=人工粘贴（manual），或未来用厂商开放接口
- 自动开播/下播：伴侣不开放自动化入口，能力=MANUAL（已在平台适配层如实声明）

## 三、关键实测数据汇总

| 指标 | 值 |
|------|-----|
| 口型推理 fps | 51~66 |
| 接收端 fps | 25.1~25.5（稳定 ≥24 ✓） |
| 温度 | 50~54℃（红线 75 ✓） |
| 显存（整卡） | 峰值 7721MiB，波动 5.9~7.7G（整卡口径 FAIL） |
| TTS 缓存命中 | 0.00s；未命中 10~19s |
| 弹幕回答延迟 | 缓存命中 <2s；新问题 >10s（未达标） |
| 无泄漏 | 15min rise=-146MiB ✓ |

## 四、剩余待办（外部条件就绪后）
1. 安装 OBS → 验证虚拟摄像头/窗口源 + NVENC 推流 + 30min 录制
2. bench 与播报并行跑一轮，补 FPS 区间证据；引入进程级显存归因（psutil + torch.cuda.memory）
3. 抖音若提供开放弹幕接口 → platform 适配层已留 real 源位
4. 质量档 MuseTalk：需 3090 环境（stage_a_3090 profile 已留）
